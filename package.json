{
  "name": "cofco-salesorder01-cap-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "cds": {
    "hana": {
      "deploy-format": "hdbtable"
    },
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      },
      "SalesOrderService": {
        "kind": "odata",
        "model": "srv/external/SalesOrderService"
      }
    }
  },
  "sapux": [
    "app/mgrsalesorder01"
  ],
  "devDependencies": {
    "@sap/ux-specification": "^1.108.1",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "undeploy": "cf undeploy cofco-salesorder01-cap --delete-services --delete-service-keys --delete-service-brokers",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1"
  }
}